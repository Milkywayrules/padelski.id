pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,tsx,json,jsonc}"
      run: bunx biome check --write {staged_files}
    typecheck:
      run: bun run typecheck

pre-push:
  commands:
    test:
      run: bun run test
